[package]
name = "gfx_dsl"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
quote = "1"
syn = { version = "2", features = ["full", "extra-traits"] }
macrokid_core = { path = "../../macrokid_core" }
gfx_dsl_support = { path = "../gfx_dsl_support" }
